<li role="presentation" class="tab-item" data-uuid="{{uuid}}">
    <a href="#calendar-{{uuid}}" class="calendar-tab-item" data-uuid="{{uuid}}" role="tab" data-toggle="tab">
        {{title}}
    </a>
    <div class="form-group calendar-title-edit" style="display: none">
        <input type="text" class="form-control" placeholder="title" value="{{title}}">
    </div>
</li>
